# Context cleanup lock — 1206 v2

- Use `calendar/` and `canon_lore/` as current sources.
- Do not use legacy academy/start data unless it is explicitly re-added to 1206 files.
- Required files are selective; if a detail is not loaded, do not invent hard canon.
- State files in session override seed state files.
