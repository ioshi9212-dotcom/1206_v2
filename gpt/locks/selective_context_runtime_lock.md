# Selective context runtime lock — 1206 v2

Required files are loaded selectively.

- Use loaded character files for active characters.
- Use runtime digest for compact state/calendar/lore slices.
- If a file is missing, do not invent detailed canon from memory.
