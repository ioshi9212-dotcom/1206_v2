# Calendar usage lock — 1206 v2

- Active calendar source: `calendar/calendar_index.yaml` + `state/calendar_runtime.json` + current day file.
- Calendar beats are hooks and constraints, not ready-made prose.
- Do not overfill a day with filler.
- If time/date/location changes, update `state/current_state.json` and `state/calendar_runtime.json`.
