# Acquaintance and named NPC state lock — 1206 v2

If a new named NPC becomes important:

- save their ID in `state/session_npcs.json` or add a proper character card later;
- record what Akira knows about them;
- do not treat them as established canon without a file.
