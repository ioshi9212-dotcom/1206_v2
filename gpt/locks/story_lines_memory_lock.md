# Story lines memory lock — 1206 v2

Use `state/story_lines.json` for:

- dated events;
- open obligations;
- long-running emotional/story lines;
- unresolved hooks;
- consequences that should matter later.

Do not create many one-scene files for small memory. Update the existing story line instead.
