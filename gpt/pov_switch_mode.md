# POV switch mode — 1206 v2

Default POV is Akira.

Non-Akira POV is allowed only if the user explicitly writes `POV:` / `пов:` with a target.

POV header line may use:

```txt
🎥 POV: {character} · Акира этого не видит напрямую
```

Do not use non-Akira POV accidentally.
