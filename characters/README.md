# Characters 1206 v2

Основной источник персонажа — `characters/<id>/character.yaml`.

Минимальный набор для старта:

- `akira`
- `jun`

Новых персонажей добавлять отдельной папкой и регистрировать в `characters/character_id_index.md`, а также в `app/compact_context_patch.py` (`NEW_CHARACTER_FOLDERS`).
